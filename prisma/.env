# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#using-environment-variables

# Prisma supports the native connection string format for PostgreSQL, MySQL and SQLite.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/mydb?schema=public"

# PG_DATABASE_NAME=crypto_currencies
# PG_DATABASE_PORT=5434
# PG_DATABASE_DOMAIN=localhost
# PG_DATABASE_LOCATION=${PG_DATABASE_DOMAIN}:${PG_DATABASE_PORT}/${PG_DATABASE_NAME}

# PG_DATABASE_USERNAME=admin
# PG_DATABASE_PASSWORD=simplePass
# PG_DATABASE_LOGIN=${PG_DATABASE_USERNAME}:${PG_DATABASE_PASSWORD}

# This is how our development DATABASE_URL looks like:
DATABASE_URL=postgresql://admin:simplePass@localhost:5434/crypto_currencies?schema=public
# But instead of setting env variable here, we set it at machine level (your computer's OS's variable) or at IDE level or
# we can even pass it via command when starting the application/server.

# Heroku provides DATABASE_URL variable automatically on machine level so it will be used over variable inside this file.
